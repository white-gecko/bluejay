modules:
  - type: dnf
    repos:
      keys:
        - https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/-/raw/master/pub.gpg
      files:
        - vscodium.repo
    install:
      packages:
        # external
        - codium
